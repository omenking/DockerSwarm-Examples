#!/usr/bin/env bash

docker context use todos-vm

CID=$(docker ps --filter "name=_db\." --format "{{.ID}}" | head -n1)

docker exec -it -u postgres "$CID" psql "postgresql://postgres:password@localhost:5432/todos_prod"
